#include<stdio.h>
int main ()
{
    int i,n,odd=0,even=0;
    printf("Enter the value of n :");
    scanf(" %d",&n);

    for(i=1;i<=n;i++)
    {
        if(%2==0)
            even=even+i;
        else
            odd= odd+i;
    }
    printf(" Summation=%d\n",odd-even);
}
